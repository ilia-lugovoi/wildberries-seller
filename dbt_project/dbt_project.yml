name: wb_analytics

version: '1.0.0'

config-version: 2

profile: wb_analytics

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]

models:
  wb_analytics:
    staging:
      +materialized: view
    
    intermediate:
      +materialized: view

    marts:
      +materialized: table